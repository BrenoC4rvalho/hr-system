services:
  database:
    container_name: "hrsystem-db"
    image: "ankane/pgvector:latest"
    env_file:
      - ./.env
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
    restart: always

volumes:
  db_data:

